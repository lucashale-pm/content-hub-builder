# Countdown

Purpose: Live countdown toward editor-selected future local date and time.

States: Optional heading; live days, hours, and minutes; stops at zero. Missing target shows heading only.

Constraints: Uses browser local time. Builder picker prevents past selections. No server time or timezone conversion.

Reuse: Uses shared brand background, ink, muted, accent, and typography tokens.
